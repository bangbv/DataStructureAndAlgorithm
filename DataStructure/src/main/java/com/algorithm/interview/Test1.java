package com.algorithm.interview;

/*
 * import static org.junit.Assert.*;
 * 
 * import java.util.LinkedList; import java.util.ListIterator;
 * 
 * import org.junit.Test;
 * 
 * public class Test1 {
 * 
 * @Test public void test() { int V = 6; Graph g = new Graph(V); g.addEdge(0,
 * 1); g.addEdge(1, 2); g.addEdge(1, 3); g.addEdge(2, 5); g.addEdge(3, 4);
 * g.addEdge(4, 5);
 * 
 * LinkedList<Integer> result = Tenpoint7.subGraph(g, 3); LinkedList<Integer>
 * expectedResult = new LinkedList<>(); expectedResult.add(3);
 * expectedResult.add(4); expectedResult.add(5);
 * 
 * ListIterator<Integer> l = result.listIterator(); ListIterator<Integer> el =
 * expectedResult.listIterator(); while(l.hasNext() && el.hasNext()) {
 * 
 * int n = l.next(); int en = el.next(); assertEquals(n, en); }
 * 
 * }
 * 
 * }
 */
